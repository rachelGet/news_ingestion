apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: scheduler-conn
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "@every 2s"
  - name: direction
    value: "input"

scopes:
  - news-transformer
